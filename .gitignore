# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
*.bak
photo_*.jpg
password.txt
password*.txt

# local skill files (not served, keep private)
# Note: SKILL.md is no longer routed (the /skill page was removed); the
# live agent training content lives in lib/train-skill.ts and is served
# via /api/skill/train and /train. SKILL.md is now an internal source-of-
# truth doc and is gitignored along with the other research notes.
/SKILL.md
/SKILL.md.bak
/SKILL-QUEST.md
public/sentineltemposkill.md

# research and planning notes (internal only — never pushed)
AGENTS.md
CLAUDE.md
DEPLOYMENT-GUIDE.md
FIX-PLAN-PROGRESS.md
MAINNET-CUTOVER-CHECKLIST.md
SECURITY-AUDIT.md
THREAT-ACTORS.md
AGENT-WALLET-BRIDGE-DESIGN.md
MPP-FEEDBACK-REPORT.md
NFT-LAUNCHPAD-PLAN.md
TEMPO-*.md
BUILD-STEPS.md
DEV-GUIDE.md
SENTINEL-TEMPO-PLAN.md
SETUP-GUIDE.md
AI-TOKEN-PROJECT-RESEARCH.md
OPENCLAW-RENDER-RESEARCH.md
QUEST-API.md
monitor.ps1

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*
!.env.example

# watcher local state (lives on Render persistent disk in prod)
.watcher-state.json

# local soak / load-test output
.soak-*.log

# pg_dump output (may contain password fragments from connection strings)
*_schema.sql
local_schema.sql
render_schema.sql

# merkle generated files (placeholders committed, regenerated for mint phase)
# config/merkle-root.json
# config/merkle-proofs.json

# old data files (migrated to PostgreSQL)
data/

# database backups and exports (contain sensitive data)
backups/
db_*.txt
keep_ids.txt

# whitelist files (private)
White lists/

# research and VPS notes
RAG-*.md
commandsVPS.md

# temp scripts
tmp_*.py
scripts/monitor-*.ps1

# local on-chain dumps (transient, regenerated by scripts/)
/all-mints.json
/chain.json

# Claude/Copilot local state (not for repo)
.claude/

# Irys probe JSON reports (transient diagnostics)
scripts/irys-probe-*.json

# vercel
.vercel

# hardhat build artifacts
contracts/artifacts/
contracts/cache/
contracts/typechain-types/
contracts/node_modules/

# typescript
*.tsbuildinfo
next-env.d.ts
